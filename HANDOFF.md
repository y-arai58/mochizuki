# HANDOFF

作業を引き継ぐ人・エージェント向けのメモ。規約は `AGENTS.md`、仕様とセットアップは `README.md`。

## いまの状態

**起動・ビルド済みです。** D1のスキーマはローカル・リモートともに適用済みで、2人用のID・パスワードログインもローカルで確認済みです。公開版には本番のSecret設定とデプロイが必要です。

含まれているもの / 含まれていないもの:

| | 状態 |
|---|---|
| 画面（2ルート）・コンポーネント・月相ロジック | あり |
| サーバー関数（D1 + 2人用ログイン） | あり |
| `schema.sql` / `wrangler.jsonc` / `vite.config.ts` | あり |
| ロゴ一式・アイコン・manifest | あり |
| `package.json` / `tsconfig.json` / `src/router.tsx` / `routeTree.gen.ts` | あり |
| D1データベース本体 | あり |

## 最初にやること

```bash
# 1. 依存関係を入れる
npm install

# 2. ローカルD1にスキーマを適用する
npm run db:local
npm run cf-typegen

# 3. .dev.vars を作る。2人分のID・パスワードと32文字以上のSESSION_SECRETを設定する
cp .dev.vars.example .dev.vars

# 4. 起動
npm run dev
```

**受け入れ条件**: `/` を開くと新月の月が表示され、鉛筆アイコンから1件記録すると月がすこし満ちて目盛りが1つ点る。`/year` に今月のセルが表示される。`npm run build` が型エラーなしで通る。

ここが通ったら、Cloudflare Dashboardで `AUTH_USERS` と `SESSION_SECRET` をSecretとして設定してデプロイ（README手順3・4）。

## 詰まりそうな箇所

TanStack Startはバージョン間の差が大きく、以下は書き換えが必要になる可能性があります。**エラーが出たら公式ドキュメントの現行版を確認してから直してください。** 私が書いた形が古い可能性のほうが高いです。

1. **`__root.tsx` の書き方** — `createRootRoute` + `head` + `HeadContent` + `Scripts` の構成。雛形が生成する `__root.tsx` と見比べて、雛形側の書き方に合わせるのが安全です。中身（ヘッダー・meta・links）はそのまま移植できます。
2. **`getRequestHeader` の import 元** — `@tanstack/react-start/server` のはず。`getRequestHeaders()` で全ヘッダを取る形に変わっていたらそちらに合わせる。
3. **`cloudflare:workers` からの `env`** — `@cloudflare/vite-plugin` 前提。効かない場合は `getPlatformProxy` 経由に切り替える必要があります。
4. **`validateSearch` / `loaderDeps`** — ルートの検索パラメータ周り。型が合わない場合は雛形の例に合わせる。
5. **`src/server/entries.ts` の `D1Database` 型** — `npm run cf-typegen` で `worker-configuration.d.ts` が生成されないと型が見つかりません。

## 設計判断とその理由

変更する前に読んでください。見た目の好みではなく、検証して決めた結果です。

**なぜ器ではなく月か** — 最初は水瓶に液体が溜まる案でした。抽象ランドスケープのデザイン言語に寄せる過程で、円＝月＝「満ちる」という語彙のほうが強いと判断。器案・入江案・水位計案・地層案なども作って比較しました。

**なぜ30件か** — 1日1つの目安。ひと月に30でちょうど満月になる。50件だと1件あたりの変化が小さすぎて増えた実感が出ませんでした。

**なぜ目盛りの環があるか** — 月相だけでは1件あたりの差が2〜3%で、パッと見て増えたと分からない。月相が情緒、目盛りが情報という役割分担です。1点＝1件なので、環が一周すると望月になります。

**なぜ満月で止まるか** — 十六夜以降の呼び名を使うには満月を過ぎて欠ける必要があり、それは「増えたのに減ったように見える」ため却下しました。

**なぜ海だけ揺れるか** — 月の中身が動くと月相が読み取れなくなる。動きは「静止しているように見えるが、よく見ると動いている」程度に抑える方針です。

**なぜロゴが満月ではないか** — 満月＝ただの円で、16pxでは識別できない。名前が到達点、マークが過程という関係にしました。

**なぜ「あいことば」が無いか** — 当初は共有グループを合言葉で作る設計でしたが、推測されるリスクがあるため、2人それぞれのID・パスワードでログインする方式にしました。共有の月はサイトに1つだけ存在し、登録済みの2人が同じ月を使います。

**なぜ `ym` をクライアントが送るか** — 月の境目をユーザーのローカル時刻で決めるため。UTC集計だと月末深夜の記録が前月に入ります。

## 未着手のタスク（優先順）

1. **本番Secretの設定とデプロイ** — これをやるまで公開してはいけない
2. **ログイン試行のレート制限** — 公開URLなので、必要になればDurable Objects等で追加を検討する
3. **入力検証をZodに** — 現在ハンドラ内で手検証。`createServerFn` の検証APIの現行名を確認してから置き換える
4. **エラー表示の整備** — 現在は画面下部のトーストのみ。ネットワーク断や401の区別ができていない
5. **Service Worker** — オフラインでも開けるようにする。記録の送信キューまで作るかは要検討
6. **望月になった日の通知**
7. **ウィジェット** — ホーム画面に今月の月を出す。ネイティブ化が必要で、Web版の範囲外

## 触っていない領域

- テストが1つもありません。`src/lib/moon.ts` は純関数なので、まずここから書くと費用対効果が高い（件数→呼び名、件数→照度、29件が満月にならないこと）
- CI/CDなし。Workers Buildsを繋ぐか GitHub Actions を書くかは未決
- 複数グループ（夫婦以外への配布）は未対応。必要なら `members` テーブルを追加して `groupId()` を書き換える設計を想定しています

## Codexに投げる最初の指示（例）

```
AGENTS.md と HANDOFF.md を読んでください。

このリポジトリは TanStack Start + Cloudflare Workers + D1 のアプリです。
HANDOFF.md の「最初にやること」に従って、
npm run dev が起動し npm run build が型エラーなしで通る状態を確認してください。

TanStack Start のAPIはバージョン差が大きいので、
エラーが出たら公式ドキュメントの現行版を確認し、
私が書いたコードのほうを直してください。

AGENTS.md の「絶対に壊してはいけないもの」は変更しないでください。
月相のロジック（src/lib/moon.ts）とキービジュアルの寸法は仕様です。
```
